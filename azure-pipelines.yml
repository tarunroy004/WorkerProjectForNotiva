trigger:
- main

pool:
  vmImage: 'windows-latest'

steps:
- script: echo Hello World from Azure DevOps Pipeline!
  displayName: 'Print Hello World'

- script: |
    echo Pipeline is running successfully
    echo Build completed
  displayName: 'Confirm pipeline execution'